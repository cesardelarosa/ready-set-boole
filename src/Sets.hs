{-# LANGUAGE NoImplicitPrelude #-}

module Sets (powerset, evalSet) where

import Data.String (String)
import GHC.Base (undefined)

powerset :: [a] -> [[a]]
powerset _ = undefined

evalSet :: String -> [[a]] -> [a]
evalSet _ _ = undefined
