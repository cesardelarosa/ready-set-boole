{-# LANGUAGE NoImplicitPrelude #-}

module Curves (map, fnReverseMap) where

import Data.Word (Word16)
import GHC.Base (undefined)
import GHC.Float (Double)

map :: Word16 -> Word16 -> Double
map _ _ = undefined

fnReverseMap :: Double -> (Word16, Word16)
fnReverseMap _ = undefined
